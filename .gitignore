# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — NEVER commit these. Credentials live here in dev and
# in Vercel's env var UI in prod. The service account key, the Anthropic
# key and the Postgres URL are all secrets.
.env
.env.local
.env.*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
