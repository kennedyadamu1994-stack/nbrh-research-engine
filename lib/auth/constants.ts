/**
 * Cookie name for the admin session. Distinct from Club House OS's
 * "admin_session" so that, if both apps ever end up on the same parent
 * domain, their cookies don't collide.
 */
export const SESSION_COOKIE_NAME = "nbrh_research_session";
